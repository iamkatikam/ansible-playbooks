---
  - name: Define variables in inventory file and read from play
    hosts: localhost
    connection: local

    tasks:
    - name: print vars info
      ansible.builtin.debug:
        msg: "course: {{ course }}, duration: {{ duration }}, trainer: {{ trainer }}"
